name: CI
on:
  push:
    branches: [master]
jobs:
  build:
    runs-on: ubuntu-18.04
    steps:
      # - name: checkout backend
      - uses: actions/checkout@v2
        with:
          fetch-depth: 1000
          repository: ${{secrets.USERNAME}}/${{secrets.BACKEND_REPO}}
          path: backend

      # - name: Testing Backend
      #   run: |
      #     cd backend
      #     npm install
      #     npm test

      # # - name: checkout frontend
      # - uses: actions/checkout@v2
      #   with:
      #     repository: ${{secrets.USERNAME}}/${{secrets.FRONTEND_REPO}}
      #     path: frontend

      # - name: Testing frontend
      #   run: |
      #     cd frontend
      #     npm install
      #     npm test

      # - name: Building frontend
      #   run: |
      #     cd frontend
      #     npm run build
      #     mv build/* ../backend/public/.
      #     cd ..
      #     rm -rf frontend

      - uses: actions/checkout@v2
        with:
          repository: ${{secrets.USERNAME}}/${{secrets.BUILD_REPO}}
          path: build

      - name: Setup SSH Keys and known_hosts
        env:
          SSH_AUTH_SOCK: /tmp/ssh_agent.sock
        run: |
          cd build
          mkdir -p ~/.ssh
          ssh-keyscan github.com >> ~/.ssh/known_hosts
          ssh-agent -a $SSH_AUTH_SOCK > /dev/null
          ssh-add - <<< "${{ secrets.DEPLOY_KEY }}"

      # - name: Some task that fetches dependencies
      #   env:
      #     SSH_AUTH_SOCK: /tmp/ssh_agent.sock
      #   run: ./fetch-deps.sh

      - name: Push to Repo
        env:
          SSH_AUTH_SOCK: /tmp/ssh_agent.sock
        run: |
          cd build
          rm -rf *
          mv ../backend/* .
          git config --global user.email "58025059+shiviraj@users.noreply.github.com"
          git config --global user.name "shiviraj"
          echo hello > hello.txt
          git add --all
          git commit -m "deployed"
          echo "committed successfully"
          git push origin master -u

      # git push origin master --set-upstream -f remote https://${{secrets.TOKEN}}:x-oauth-basic@github.com/shiviraj/counterBuild.git
      # git remote add origin git@github.com:shiviraj/counterBuild.git
      # git remote add heroku https://heroku:${{secrets.HEROKU_API_KEY}}@git.heroku.com/shivi-counter.git

# git remote add origin https://${{secrets.TOKEN}}:x-oauth-basic@github.com/shiviraj/counterBuild.git
# curl -u "shiviraj:${{secrets.TOKEN}}" https://github.com/shiviraj/counterBuild.git
# git remote set-url --add origin https://shiviraj:${{secrets.TOKEN}}@github.com/shiviraj/counterBuild.git
